name: PR
on: pull_request
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Codes
        uses: actions/checkout@v4

      - name: build
        run: npm run build

      - name: test
        run: npm run test
